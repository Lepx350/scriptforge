DATABASE_URL="postgresql://user:password@host:5432/dbname"
GEMINI_API_KEY="optional — can also be set in-app via Settings page"
BLOB_READ_WRITE_TOKEN="required for Vercel Blob image storage — get from Vercel dashboard > Storage > Blob"
